spring:
  application:
    name: chatu
  datasource:
    url: jdbc:mysql://localhost:3306/chatu?createDatabaseIfNotExist=true 
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

websiteUrl: http://localhost:8080/